/Reverse a 'n'digit number
//Reverse a singly linked list
//Delete the nth element from an array of 'm'no.s.   n less than equal to m.
//Insert an element at nth position in an array.
//Find the no. of words in a given sentence.
//Write the codes without using in-limit function and then 
//calculate the time complexity in each can.